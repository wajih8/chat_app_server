def fetch_row(rows):

    for row in rows:
        pass
    try:
        return row[0]
    except:
        return


def fetch_row2(rows):

    for row in rows:
        pass
    try:
        return row
    except:
        return


def fetch_rows(rows):
    t = []
    for row in rows:
        t.append(row)
    return t
